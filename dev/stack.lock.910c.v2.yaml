# =====================================================================
# 运行时层 · 910C 原型统一基座 —— 锁定镜像与基座规范
# 文件：dev/stack.lock.910c.v2.yaml
# 效力：本文件是全组结论性验证的唯一环境依据；任何变更需更新本文件并知会全组
# 起草：device-context（Kistich）提案 ｜ 总组定稿 ｜ 依据：运行时层原型验证-战略目标-910C
# 记录：2026-09-08 ｜ 定稿：2026-09-09 ｜ v2 更新：2026-09-12 ｜ 状态：生效
# 版本化规范见战略文档 §8（后续如需归档旧版本，按该规范执行）
#
# 本版本相对 v1 的变化：仅新增 candidates: 段，登记训练腿候选新血统基座镜像
# （dev/images/*/v2/，按 BAAI·FlagTree 官方 ascend3.5 手册路径重建）。
# lock: / rules: / per_leg: 与 v1 逐字节一致 —— 未发生任何切换，训练腿结论性
# 验证仍用 lock.train.image 指向的现有镜像。是否切换到候选血统由总组另行裁定。
# =====================================================================

version: 2
recorded_at: "2026-09-09"
updated_at: "2026-09-12"
owner: 总组
chip: "昇腾 910C"
acceptance_model: "Qwen/Qwen3-Embedding-0.6B"
flagtree_line: |
  ascend3.5 —— 两个锁定镜像的整套栈（CANN 9.0.0 / Python 3.11 / torch 2.10.0 /
  triton 3.5.0 / vLLM 0.20.2）逐项对齐 FlagTree ascend 手册的 ascend3.5 线。
  手册：https://github.com/flagos-ai/FlagTree/wiki/User-manual-for-ascend
  triton_ascend 3.2.1 是华为昇腾 Triton 插件（triton-ascend，与 triton 是两个包）的
  版本号，wheel 取自 vllm-ascend:v0.20.2rc1-a3，import 自报 3.2.0，不代表 ascend3.2 线。
  构建方式未按手册（无 FlagTree/triton 构建物）；FlagTree fork 分支本原型不需变更，
  日后 FlagTree 构建物若要进 ascend3.5 镜像须先切 triton_v3.5.x。详见 dev/images/image_list.md。
  2026-09-12 补记：训练腿已产出按手册路径真正构建的候选新血统（见下方 candidates:），
  本段落描述的仍是当前生效锁定镜像（lock.train）的构建方式，未变。

# ---------------------------------------------------------------
# 设备注册路线（全组原则）
# ---------------------------------------------------------------
device_registration:
  primary: "Route A（各芯片厂商官方设备插件；昇腾 = 华为昇腾官方 torch_npu）"
  exception: |
    训练腿因锁定镜像的环境约束，本月使用 flagos（torch_fl）接入，
    为该镜像下的权宜例外，不代表路线变更。TODO：10 月起评估训练腿切回 Route A 的成本。

# ---------------------------------------------------------------
# 锁定镜像（两腿）—— 与 v1 完全一致，未切换
# ---------------------------------------------------------------
lock:
  train:                                # 训练腿：2 卡分布式微调
    image: "flagrt/ascend-operator-runtime-comm:0.1.3-cann9.0-py311-torch2.10-flagcx0.13.0g55eb2ffp2-arm64"
    notes: "容器内补装 transformers；通信库与算子库已内置。release.stage=candidate（torch_fl 例外线，非厂商官方主线；华为昇腾官方主线是 torch_npu）"
    container_name: "flagos-proto-train-910c"
    base_image_entry: "dev/images/ascend-train-comm/v1"        # 血统 / 内置版本 / 重建配方 / 离线归档指针
    registry_status: "组内镜像，无 registry 要求；构造与重建见 base_image_entry（repro_status 🟢 functional-repro，2026-09-10 实机验证）"
  infer:                                # 推理腿：单卡推理服务
    image: "quay.io/ascend/vllm-ascend:v0.20.2rc1-a3"
    notes: "推理框架 0.20.2 华为昇腾官方镜像自带"
    container_name: "flagos-proto-infer-910c"
    base_image_entry: "dev/images/ascend-infer-vllm/v1"
    registry_status: "华为昇腾官方镜像，已验证可拉取（2026-09-09，digest sha256:5cf8a2b6db8b06eb1bc7fc7d191d667aebf2b197351bdba13f776918c11ec7a7）｜ repro_status 🟢"

# ---------------------------------------------------------------
# 候选血统（新增，2026-09-12）—— 仅登记，不生效，不改变上面 lock: 的结论性验证依据
# ---------------------------------------------------------------
candidates:
  train:
    status: "candidate-not-effective（训练腿现网仍以 lock.train.image 为准；本条目仅供总组评估切换用）"
    lineage: "按 BAAI·FlagTree 官方 ascend3.5 手册路径重建：官方预构建基座镜像 + 上游 triton_v3.5.x pin commit 现场编译（取代 v1 的 pip triton wheel 组合） + Torch-FL / FlagGems / FlagCX 均用 FlagRT 组织仓公开主干 commit（FlagCX 不再依赖 owner 私有 commit，无 gaps）"
    images:
      operator_runtime: "flagrt/ascend-operator-runtime:1.0.0-flagtree3.5-cann9.0-py311-torch2.10-arm64"
      operator_runtime_comm: "flagrt/ascend-operator-runtime-comm:1.0.0-flagtree3.5-cann9.0-py311-torch2.10-flagcx0.13.0g4e0e0cb-arm64"
    base_image_entry: "dev/images/ascend-train-comm/v2（父层 dev/images/ascend-operator-runtime/v2）"
    repro_status: "🟢 functional-repro；真机 2 卡 all_reduce/all_gather/p2p/async_all_reduce 40/40 通过"
    known_issues: "通信验证跑完后进程退出阶段偶发 free(): invalid pointer SIGABRT，不影响通信正确性本身；详见 dev/images/ascend-train-comm/v2/lock.yaml:known_issues"
    persisted_at: "本机 docker images 已注册；docker save 落地 /mnt/raid/xliu969/flagrt-image-backup/20260912/（tar.gz + SHA256SUMS）"
    next_step: "是否切换 lock.train 到本候选血统，由总组另行裁定（需先评估 known_issues 与两段 demo 联调影响）"

# ---------------------------------------------------------------
# 基座组件（设备上下文已交付，全组下游使用）
# ---------------------------------------------------------------
runtime:
  component: "统一运行时 API + Backend 注册表 + 昇腾后端"
  version: "0.1.0"
  location: "dev/device-context/runtime/"
  entry: "runtime.use(\"ascend\") 一行接入；接口见 docs/INTERFACE_CONTRACT_DC_20260908.md"
  conformance_baseline: "13/13 + 6/6（910C 真机）"
  feedback: "问题反馈 device-context，按周迭代组件版本"

# ---------------------------------------------------------------
# 使用规则（第 1 条为最高优先级，实测取证，出现事故先查它）
# ---------------------------------------------------------------
rules:
  - |
    【最高优先级】带卡容器并发上限 3：同一时刻最多 3 个挂载 NPU 设备的容器同时运行。
    超限后新容器内 acl.init() 返回 500000（ACL_ERROR_INTERNAL_ERROR），
    伴随 DrvMngGetConsoleLogLevel failed(ret=4) / path string is NULL，
    表现为 torch.npu.device_count()=0、设备"凭空消失"。
    → 出现 500000 / 设备不可见，第一时间核查并发容器数（docker ps 数带卡容器），
      不要先怀疑镜像、驱动或代码。
    → 两条腿（训练/推理）不需要同时跑：串行执行即可，跑完一条停掉再起下一条。
  - "结论性验证只在锁定镜像内做；日常调试容器结果不作为验收依据"
  - "任何镜像变更必须更新本文件并知会全组"
  - "起容器前自查：并发容器数 ≤3、镜像与本文件一致（含 registry_status）、容器名符合规范、卡资源空闲"
  - "合入把关五条：镜像与 stack.lock 一致 ｜ 容器/分支命名合规 ｜ 接口约定章节完稿且下游确认 ｜ 能跑自己那段 demo ｜ 无环境分叉"
  - |
    基座镜像入锁门槛：任何镜像进入本文件 lock: 前，dev/images/<name>/v<N>/ 必须存在且含 lock.yaml，
    repro_status ≥ 🟡（配方在手；组内镜像不涉及 registry，看能否凭仓内 Dockerfile.repro+assets 重建）；
    上游发布的镜像（华为昇腾官方 / 社区）可无 Dockerfile 但须有可拉取 digest。规范见 dev/images/README.md。
    统一基座的确定/调整/发布由总组裁定（依据各子方向 STATUS.md 暴露的问题与意见）；各方向只消费不自建
    （用锁定 tag 的本机镜像或执行方的 docker save 包），诉求写进本方向 STATUS.md 由总组收拢。

# ---------------------------------------------------------------
# 两条腿的运行方式约束（实测）
# ---------------------------------------------------------------
per_leg:
  train:
    device_backend: "flagos（torch_fl）"
    note: |
      该镜像禁止 torch_npu 与 Torch-FL 运行时共存（自带校验脚本会直接报错）；
      必须 TORCH_DEVICE_BACKEND_AUTOLOAD=0 且先 import torch_fl 再 import torch。
      我们的原型通过 runtime/backends/flagos 后端接入。
    python: "/usr/local/python3.11.15/bin/python3"
  infer:
    device_backend: "npu（torch_npu）"
    note: "镜像自带 torch_npu，我们的原型通过 runtime/backends/ascend 后端接入"
  concurrency: "两条腿串行；确需并行时须确保带卡容器总数 ≤3"

# ---------------------------------------------------------------
# 变更记录
# ---------------------------------------------------------------
changelog:
  - date: "2026-09-08"
    who: "device-context"
    what: "初版锁定（依据战略文档 §4）"
  - date: "2026-09-08"
    who: "device-context"
    what: |
      置顶新增「带卡容器并发上限 3」规则（实测：4 个并发时 acl.init()=500000，
      降到 3 个后立即恢复）；补充两条腿的设备后端差异与串行要求
  - date: "2026-09-09"
    who: "总组"
    what: |
      总组定稿：owner 改为总组；训练腿镜像核实为临时机器绑定资产并标注获取方式与 TODO；
      推理腿镜像核实为官方镜像并记录 digest；新增设备注册路线声明
      （Route A 主线，训练腿 torch_fl 为本月例外）。
  - date: "2026-09-09"
    who: "runtime-team"
    what: |
      新增 dev/images/ 基座镜像规范：从 npu1-27 本地镜像还原训练腿两层（ascend-train-comm /
      ascend-operator-runtime）的 Dockerfile 与 pin 清单，标注官方/自定义边界；每个 lock 条目加
      base_image_entry 指针；新增「基座镜像入锁门槛」规则。训练腿 repro_status 由黑盒升至 🟡。
  - date: "2026-09-09"
    who: "总组"
    what: |
      按 dev/images/TODO.md 推进一轮：docker save 两镜像止血备份（/mnt/raid/xliu969/flagrt-image-backup/
      20260909，非仓库；raid 盘，抗 prune）；查明 CANN 基座 = harbor pytorch-plugin-fl:manual-20260807-
      ascend-dev@sha256:a36a3022；从现镜像回收 wheelhouse(36 wheel)/flagcx 已装包/DSA 覆盖文件；补齐
      mpich 源码；pin 源码 commit 就位。缺口收敛到两项 owner 独占资产（FlagCX 私有 commit 55eb2ff +
      2 个 patch）。未改 lock: 镜像；仍锁原预构建镜像。详见 dev/images/TODO.md。
  - date: "2026-09-10"
    who: "runtime-team"
    what: |
      新增 flagtree_line 字段：核对 FlagTree ascend 手册后确认两个锁定镜像全栈为 ascend3.5 线；
      更正此前"triton_ascend 3.2.1 = ascend3.2 线"的误读（那是华为插件的版本号，非 FlagTree 线号）。
      同步 dev/images/image_list.md 版本线章节与战略文档 §4/§7。未改镜像。
  - date: "2026-09-10"
    who: "总组"
    what: |
      功能等价重建完成并验证：各 dev/images/*/v1/Dockerfile.repro 实机构建出重建镜像，
      pip freeze 与原镜像逐行一致、FlagCX .so 逐字节一致（见各 dev/images/*/v1/REBUILD.md）。
      两镜像 repro_status → 🟢。明确：组内镜像无 registry 推送要求，可复现性 = 凭仓内
      Dockerfile.repro + assets 重建。逐字节复现为可选金标准，需 owner 交出 FlagCX 私有
      commit+patch。lock: 镜像 tag 不变。dev/images/ 同日重构为纯归档/索引/重建目录，
      去除消费方定位/路线/治理内容（那些留在本文件与 docs/）。
  - date: "2026-09-12"
    who: "总组"
    what: |
      新建 v2：新增 candidates.train 段，登记按 BAAI·FlagTree 官方 ascend3.5 手册路径
      重建的训练腿候选新血统基座镜像（dev/images/ascend-operator-runtime/v2 + ascend-train-comm/v2）。
      本次仅记录，不切换：lock.train.image 与 v1 完全一致，容器名/规则/per_leg 均未变。
      候选血统已本机 docker images 注册 + docker save 持久化到 raid；真机 2 卡通信验证
      40/40 通过；已知一处退出期 SIGABRT 待定位（不影响通信正确性）。v1 标记 LEGACY，
      取代原因见 v1 文件头部说明。
