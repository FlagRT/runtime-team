# 910C 显存画像报告（骨架 · 待测填充）

> 状态：🟡 **骨架**，待带卡容器 slot 到位后按本结构填数 ｜ 起草：2026-09-10 ｜ 负责人：xliu969
> 对应：战略文档 §3 验收标准第 4 条、§5 memory 第 1 条 ｜ 权威方案：[路线A-显存与缓存管理-方案-20260822](路线A-显存与缓存管理-方案-20260822.md)
> 对照基线：[archive/V1-显存画像报告-20260817](archive/V1-显存画像报告-20260817.md)（910C 旧栈，torch_fl/Qwen3-4B，已冻结）
> ⚠️ 所有数值单元格标 **待测**；探针为 UNTESTED，见 probes/infer910c_*.py 头注。

---

## 1. 环境（锁定推理镜像栈）

| 项 | 值 | 来源 |
|---|---|---|
| 镜像 | `quay.io/ascend/vllm-ascend:v0.20.2rc1-a3`（digest `sha256:5cf8a2b6…c11ec7a7`） | dev/stack.lock.910c.v1.yaml |
| 容器名 | `flagos-proto-infer-910c` | 同上 |
| 设备后端 | npu（torch_npu），Route A 华为昇腾官方纯栈（无 FlagOS/FlagGems/FlagCX/torch_fl） | 同上 |
| Python | 3.11.15 | 实测（只读核对） |
| vLLM / vllm_ascend | 0.20.2 / 0.20.2rc1 | 同上 |
| torch / torch_npu | 2.10.0 / 2.10.0 | 同上 |
| transformers / triton | 5.5.3 / 3.2.0（triton-ascend 插件自报 3.2.0） | 同上 |
| CANN | 9.0.0 | 同上 |
| SOC_VERSION | ascend910_9391 | 同上 |
| 单芯 HBM total | 65536 MiB（64 GiB） | 同上 |
| 验收模型 | `Qwen/Qwen3-Embedding-0.6B`（EMBEDDING / pooling runner，非生成式，无 decode，几乎无 KV 增长） | 战略文档 §3 |
| 权重路径（宿主） | `/mnt/raid/hliu553/models/Qwen3-Embedding-0.6B`（或 `/mnt/raid/kangkai/hf_cache/hub/models--Qwen--Qwen3-Embedding-0.6B`） | — |
| 关键运行参数 | gpu_memory_utilization=**待测**、max_num_seqs=**待测**、max_model_len=**待测**、enforce_eager/ACLGraph=**待测**、PYTORCH_NPU_ALLOC_CONF=**待测** | — |

## 2. 方法

- **设备级 HBM 采样为真值**：外挂 `probes/infer910c_hbm_sampler.py`（宿主机轮询 `npu-smi info`，解析 per-chip HBM-Usage MB + AICore%，按 `--interval` 写时间戳 CSV，`--tag` 区分 run）。
- **vLLM 日志解析**：`probes/infer910c_mem_profile.py` 抓 `Available KV cache memory` / `GPU KV cache size` / `Maximum concurrency` / model weights / peak memory 等行。
- **driver 侧 torch_npu 计数仅作佐证**：`torch_npu.npu.memory_allocated / max_memory_allocated / memory_reserved / max_memory_reserved` 在 driver 进程读取 —— **预期 ≈ 0**，因 EngineCore 是 spawn 子进程，主进程读不到 worker 分配器状态（沿用 archive/V1 §3.3）。
- **必须先预热**：测量前先跑 `--warmup N` 轮短请求，避开首次 kernel 初始化长尾（旧栈曾达 437s）。
- **A/B 对照**：`probes/infer910c_ab_matrix.py` 跨 `gpu-mem-util` / `alloc-conf` / `enforce-eager` / `max-num-seqs` 轴批量跑，汇总 CSV + Markdown。
- 时间戳对齐：sampler CSV 的 `tag` 与 profile JSON 的 `tag` 同名，事后按时间窗切曲线。

## 3. 加载阶段画像

| 项 | 值 | 来源 |
|---|---|---|
| HBM total | 64 GiB（65536 MiB） | npu-smi |
| 模型权重占用 | **待测**（理论：0.6B 参 × 2B bf16 ≈ 1.2 GiB） | vLLM 日志 model weights 行 |
| vLLM 预分配（KV / pooling 池 + 图工作区 + 激活余量） | **待测**（embedding 模型 KV/pooling 池可能接近 0） | vLLM 日志 Available KV cache / GPU KV cache size |
| 加载后 HBM used（npu-smi 口径） | **待测** | sampler CSV（加载完成时刻） |
| 加载后 HBM 占比 | **待测** | 计算 |
| 加载时长（进程启动 → HBM 平稳） | **待测** | sampler 时间戳 / 脚本计时 |
| 其中：权重加载耗时 | **待测** | vLLM 日志 default_loader |
| 其中：engine init（profile + KV/pool 创建 + ACLGraph capture） | **待测** | vLLM 日志 core.py |
| 预热耗时（`--warmup` N 轮短请求，含首次 kernel 初始化） | **待测** | 脚本计时 |
| driver 侧 torch_npu max_memory_reserved | **待测（预期 ≈ 0）** | profile JSON |

## 4. 运行阶段峰值画像

矩阵：batch × seq-len × concurrency（具体档位待定，建议 batch ∈ {8,32,64,128}，seq-len ∈ {64,512,2048,8192}）。

| batch | seq-len | concurrency | HBM 峰值（MB） | 相对加载后增量（激活） | 单请求峰值增量 | 备注 |
|---|---|---|---|---|---|---|
| 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | — |

- HBM 峰值曲线：**待测**（sampler CSV 画时间序列，标注 warmup / 各 batch 段）。
- 激活增量（峰值 − 加载后 used）：**待测**。
- AICore% 与 HBM 峰值的相关性：**待测**。
- 是否观测到运行期新增分配 / 尖峰（还是全程恒平，如 P800 eager）：**待测**。

## 5. 碎片与 reserved-vs-allocated

| 项 | 值 | 来源 |
|---|---|---|
| reserved − allocated（碎片冗余） | **待测** | vLLM 日志 / driver（注意 driver ≈ 0） |
| 碎片冗余占比 | **待测** | 计算 |
| `PYTORCH_NPU_ALLOC_CONF=expandable_segments:True` vs 默认 对照 | **待测** | ab_matrix `--axis alloc-conf` |
| ACLGraph capture 工作区占用（enforce_eager 对照） | **待测** | ab_matrix `--axis enforce-eager` |

## 6. 与旧栈基线对照

| 指标 | 910C 旧栈 V1（archive，2026-08-17，torch_fl / Qwen3-4B） | 本次（锁定镜像 / Qwen3-Embedding-0.6B） | 说明 |
|---|---|---|---|
| 模型 | Qwen3-4B（生成式，dense） | Qwen3-Embedding-0.6B（**embedding，非生成式**） | 不可直接比 |
| 加载后占用 | 31.89 GiB（24.6s） | **待测** | — |
| KV 预分配 | 170,224 tokens / ~24.3 GiB（占加载后 76%） | **待测（预期极小或 0）** | embedding 无 decode，无 KV 增长；pooling 池远小于生成式 KV 池 |
| 最大并发 | — | **待测** | — |
| 预热（首次 attention） | 437s | **待测** | — |
| 长序列 prefill（2048×4 并发） | 22min 未完成（旧栈 P0） | **待测** | 新栈 + 小模型，预期不复现 |

**为何 embedding 0.6B 与旧栈完全不同**：① 参数量 4B→0.6B，权重 ~24.3GiB→~1.2GiB；② 无自回归 decode → 无 KV cache 逐步增长，vLLM 的 KV/pooling 预分配对 embedding 走的是 pooling 路径，池尺寸与"最大 batch × seq-len 的激活"同量级而非"并发 × 上下文长度 × 层数"；③ 峰值构成从"权重 + KV 池（大头）"变为"权重 + 单批激活 + graph 工作区"。

## 7. 结论

- 显存池定义（torch_npu caching allocator 底座 + vLLM 层）：**待填**（另见显存池定义文档）。
- 安全 gpu_memory_utilization / max_num_seqs 区间建议（供 device-context / 调度）：**待填**。
- A/B 对照要点：**待填**。
- 备注：对非生成式 embedding 模型，**"峰值" ≈ 权重 + 最大 batch 激活 + graph capture 工作区**，KV/pooling 预分配池可能接近 0 —— 本期硬性指标是"峰值画像 + 显存池定义"，KV 分层/溢出验证按战略文档 §7 不作硬指标。

## 8. 原始数据位置与复现命令

- sampler CSV：`dev/memory/benchmarks/out/infer910c_hbm.csv`（**待生成**）
- profile JSON：`dev/memory/benchmarks/out/infer910c_mem_*.json`（**待生成**）
- A/B 汇总：`dev/memory/benchmarks/out/ab_*/ab_summary.{csv,md}`（**待生成**）
- 探针：`dev/memory/probes/infer910c_hbm_sampler.py` / `infer910c_mem_profile.py` / `infer910c_ab_matrix.py`（UNTESTED）
- 复现命令：见 `infer910c_mem_profile.py` 头注「容器运行命令」段（含 `docker run ... --name flagos-proto-infer-910c ...`）。
