# 多卡执行与通信 @尤联忠

基于设备方向已有修复，完成 FlagCX 回调等待死锁和 CANN Event 资源释放两项改动的独立整理，保留原作者来源，并在统一锁定的 910C 训练容器中完成核心库、Torch 插件编译，均成功生成动态库。Event 生命周期的主机模拟测试完成 1000 次创建/释放配对；针对候选镜像“数值校验通过后退出崩溃”的问题，补齐通信结果、通信组销毁和进程退出的联合验收，14 项主机回归全部通过。同时补齐通信组生命周期、异步完成语义和能力声明，形成接口约定待确认稿，符合阶段目标中“通信修复整理与接口约定”的推进要求。（[补丁包](../patches/flagcx/)、[修复与测试记录](LOCAL_PROGRESS_20260916.md)、[接口约定](INTERFACE_CONTRACT_COMM_v1.md)）

双卡实机回归在 ACL 初始化阶段遇到资源占用错误。通过独立 ACL 检查及仅挂载空闲卡的对照实验，确认驱动返回 `87 RESOURCE_OCCUPIED`，内核记录跨容器设备命名空间冲突；本轮复查仍可复现。本轮尚未进入通信数值校验，补丁的设备运行效果仍需在占用释放后验证。编译和失败日志已归档，提交至个人分支供审查；设备回归与下游确认完成前，不作为公共分支验收通过的交付。（[服务器验证记录](SERVER_VALIDATION_20260916.md)、[目标逐项核对](GOAL_ALIGNMENT_20260916.md)）

## 下周计划

本次分支发布代码、补丁和实验结论摘要；原始服务器日志保留在本地。

协调设备占用释放后的测试窗口，在锁定环境中完成两项修复的双卡通信、跨流依赖和异常退出回归；结合结果与设备、调度方向确认通信接口约定，完成本轮改动的验证与交付。
