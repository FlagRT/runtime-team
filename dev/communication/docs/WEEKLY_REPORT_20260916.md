# 多卡执行与通信 @尤联忠

基于设备方向已有修复，完成 FlagCX 回调等待死锁和 CANN Event 资源释放两项改动的独立整理，保留原作者来源，并在统一锁定的 910C 训练容器中完成核心库、Torch 插件编译，均成功生成动态库。Event 生命周期的主机模拟测试完成 1000 次创建/释放配对；针对候选镜像“数值校验通过后退出崩溃”的问题，补齐通信结果、通信组销毁和进程退出的联合验收，14 项主机回归全部通过。同时补齐通信组生命周期、异步完成语义和能力声明，形成接口约定待确认稿，符合阶段目标中“通信修复整理与接口约定”的推进要求。（[补丁包](../patches/flagcx/)、[修复与测试记录](LOCAL_PROGRESS_20260916.md)、[接口约定](INTERFACE_CONTRACT_COMM_v1.md)）

双卡回归遇到“无计算进程但 ACL 初始化失败”的环境问题，已从独立 ACL 检查、驱动错误码追到命名空间登记：16 个逻辑设备仍归属于同一个存活容器，驱动按挂载命名空间判定独占，因此只选空闲卡仍返回 `87 RESOURCE_OCCUPIED`。已补充可复用的归属诊断工具，明确需要释放的是容器登记而不是计算负载。本轮尚未进入通信数值校验，不能将编译通过写成补丁设备回归通过；代码、补丁与结论摘要提交个人分支，原始日志仅本地保留。（[服务器验证记录](SERVER_VALIDATION_20260916.md)、[目标逐项核对](GOAL_ALIGNMENT_20260916.md)）

## 下周计划

协调已定位容器的临时释放，在锁定环境中补齐修复前后的双卡通信、跨流依赖、异常退出及训练腿回归；结合实测结果与设备、调度方向确认接口约定，满足验收门槛后再合入公共分支。
