# 与阶段目标逐项对齐

依据 `dev-1.0@f4d0ddd` 的 [阶段目标](../../../docs/运行时层原型验证-战略目标-910C.v1.md)，
并参考设备方向 `kistich/device-context@f81adaf`。
结论：本周任务方向符合 §5，尚未满足最终验收及公共 dev 合入门槛。

| 要求 | 当前证据与状态 |
| --- | --- |
| 统一基座 | 使用 stack.lock v2 的 lock.train，未切换候选镜像 |
| 通信库干净改动 | 两项独立补丁，核心库/插件编译通过；设备回归和其余补丁依赖待完成 |
| 生命周期、操作、异步、能力声明 | 接口约定章节已覆盖；仍待下游确认和定稿 |
| 双卡训练 loss 与通信对照 | 设备方向已有两 Rank 6/6 和 50 步曲线；本周未完成补丁后的训练复测 |
| 真实错误识别→分级→恢复 | 14 项主机验收测试不能替代设备闭环；当前只完成真实初始化错误的定位 |
| 至少一个下游确认可用 | 尚无人员确认记录，不能自签 |
| 无环境分叉 | 仅独立编译，未替换原安装；临时诊断实例删除，原容器恢复 |

训练腿已有结果见设备方向 `prototype/runtime/proto/train_leg_result_rank0.json`、
`train_leg_result_rank1.json`：50 步，两 Rank loss 分别从 15.4497 降到 11.1497/11.1541，
总吞吐估计约 2117 tok/s（不可把两端的 total 再相加）。这些是设备方向既有联合证据，
不计入本周本人新增实验，也不能用于证明本周补丁运行无回归。

剩余任务：协调设备命名空间占用释放；执行补丁加载身份确认及前后对照、双卡正确性、
跨流与故障退出、训练腿回归；由设备/调度方向确认接口。
原始日志仅本地保留，个人分支发布代码和结论摘要，不表示验收通过。
