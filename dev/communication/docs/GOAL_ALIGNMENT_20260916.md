# 与阶段目标逐项对齐

依据 `dev-1.0@f4d0ddd` 的[阶段目标](../../../docs/运行时层原型验证-战略目标-910C.v1.md)，
参考设备方向 `kistich/device-context@f81adaf`，本轮再次 fetch 后二者未变。
结论：本方向修复、接口文稿、双卡训练回归和故障重建证据已补齐；公共分支合入仍需下游确认，
不能代替总组宣告整个运行时原型验收完成。

| 要求 | 当前证据与状态 |
| --- | --- |
| 统一基座 | stack.lock v2 的 lock.train，原镜像不变；新库仅通过临时路径加载，不切候选镜像 |
| 通信库干净改动 | 四份独立补丁，最终 133dfba；编译、通信与训练回归通过，保留原作者来源 |
| 生命周期、操作、异步、能力声明 | 接口章节按实测更新；保守同步、超时及未覆盖能力明确，待下游确认 |
| 双卡训练 loss 与通信对照 | 本轮修复库两端 6/6、50 步 loss 15.4497→11.1547/11.1442；三类通信对照正确 |
| 真实错误识别→分级→恢复 | 真机 Rank 退出→监督器捕获→统一 API 低置信度 L3→全组重建→1600/1600；待监控方向确认口径 |
| 至少一个下游确认可用 | 尚无人员确认记录，不能自签；为当前 dev 合入阻塞项 |
| 无环境分叉 | 原安装库未覆盖，未修改宿主驱动或镜像锁；测试后原服务容器恢复运行、训练容器恢复停止 |

训练算法复用设备方向已有资产，本方向本周贡献是新库下的独立回归和身份/结果记录，不将算法
或历史 2117 tok/s 数据重新认领。本轮原库对照约 2245 tok/s，新修复库约 2047 tok/s；
仅为各一次观测，不构成性能改善或稳定退化比例结论。

还需下游确认通信接口和故障处理策略，并由监控/performance 收入组级统一证据。
异构 proxy 专项、更大卡数、精确错误码映射和异步重叠性能仍属未关闭项。
本次只发布个人分支的代码、补丁与脱敏结果，原始日志留在本地。
