# 多卡通信（communication）子方向环境变量配置
# 复制为 .env 后按需调整；.env 不入仓。公共配置见 dev/compose.base.yml。

# FlagCX 日志级别：VERSION / WARN / INFO / ABORT / TRACE。
FLAGCX_DEBUG=WARN

# FlagCX 日志子系统；联调时可临时改为 ALL。
FLAGCX_DEBUG_SUBSYS=INIT,ENV
