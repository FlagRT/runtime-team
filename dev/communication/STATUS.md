# 通信方向状态

_本轮范围：DeepFM双卡数据并行训练接入；开发态交付，待下游联合确认。_

---

## 📊 已完成

复用公共DeepFM v1与设备方向 `bdefeee` 原型，接入数据分片、梯度平均观测、全局batch参考和跨Rank参数检查。
910C/HCCL原生与观测模式各5步预热、50步训练，两个启动器退出0；每端17/17组梯度对照通过，
最大梯度误差4.66e-9、更新误差3.73e-9，最终跨Rank参数差0；两模式loss最大差2.38e-7。
观测模式每端100次梯度AllReduce、84,290,400字节输入载荷，不是链路流量。
[实测摘要](results/20260923-npu/summary.json)与[复现说明](docs/DEEPFM_INTEGRATION_20260923.md)已归档。

本批提供14项新增主机单测；个人分支包含历史用例的39项总回归已通过，但未把历史25项及其实现一起合入。
CPU/Gloo两模式各50步证据独立保存，不代替NPU验收。

## ⚠️ 范围与待办

用户确认采用最新Route A，使用独立venv中的torch 2.11.0+cu130、torch_npu 2.11.0。
本批仅含DeepFM代码、对应测试、CPU/NPU证据和文档，不带入上一周FlagCX补丁、公共镜像锁修改或设备原型整分支。
设备原型通过 `--runtime-root` 显式指向已验证版本，不能默认使用尚未同步的公共目录替代。
下游确认、重复性能统计、接口联调及公共基座口径仍待收口。
不包含真实CTR数据集收敛、性能收益、异步重叠、故障恢复新实测或OneRec训练。
