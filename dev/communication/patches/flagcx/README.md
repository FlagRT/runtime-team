# FlagCX 修复交付包

目标仓库 `FlagRT/FlagCX`，基线 `4e0e0cbcbf721169ca82348080f8353aebfe2c31`。
四份补丁由本地提交导出，最终源码 `133dfba`。前两份保留 Kistich 来源/共同署名，
后两份来自本轮实机排障；不把前期原始定位重新认领。

1. `0001-*` / `8c836df`：将 host callback 的等待移至 groupLaunch 主线程。
2. `0002-*` / `8b15547`：CANN Event 析构释放、空初始化、禁止复制，附主机替身测试。
3. `0003-*` / `af5640d`：借用运行时 ACL 流时分配独立包装，只释放包装，不销毁借用流。
4. `0004-*` / `133dfba`：CANN Host wait 和 AllGather 复制前后同步，修复所测完成/消费顺序。

在独立、干净的精确基线上按编号顺序 `git am` 四个文件。
不要对镜像源码版本不明的目录盲目应用：原镜像私有 `55eb2ff` 不是本包基线。

最终组合已在锁定环境独立编译并通过实机回归：
1600/1600 小张量、960/960 三档尺寸、240/240 跨流、Event 1000 次、
训练两端 6/6、成员退出后新组恢复。失败中间结果也保留，见[完整记录](../../docs/SERVER_VALIDATION_20260916.md)。

0004 是保守同步方案，有 Host/设备同步开销，不宣称非阻塞完成或异步重叠；
0001 的同构回归不代表异构 proxy 专项验收。仍需下游确认。
未改锁定镜像或覆盖原安装库；不直接推送 FlagCX 子仓或公共 dev，
仅将补丁包提交至 runtime-team 指定个人分支。
