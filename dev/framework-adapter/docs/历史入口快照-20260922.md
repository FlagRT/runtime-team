# 整理前入口快照（2026-09-22）

仅用于追溯，不代表当前状态。以下保存整理前README、STATUS、HANDOFF的全文；相对链接仅按归档位置补一层路径，其他内容保留。旧阻塞/版本/下一步可能已被后续结果替代。

---

## 原文件：README.md

# 框架接入与算子调用适配（framework-adapter）

> 负责人：顾宬（cgu135）｜当前阶段：NPU模型SiLU接入与受控回退原型通过，RMSNorm精度待对齐｜更新：2026-09-22

## 最新结果（2026-09-22）

真实Qwen3-Embedding-0.6B的FP16 NPU原生基线已通过；新增作用域适配15项控制测试通过，8组目标算子对照和4类回退检查通过。模型两组输入在关闭/SiLU-only两模式下通过；RMSNorm目标实现已命中，但模型隐藏状态对照失败，默认保留原生。详见[报告与原始证据](../docs/910C模型接入与安全回退-20260922.md)。

新入口：`bash dev/framework-adapter/probes/run_checks.sh qwen-controls`、`qwen-gems --model ... --runtime-root ... --dtype float16 --out 新文件.json`。运行前准备报告中的本人目录依赖，并重新确认资源。旧“本月只出方案”、初始化阻塞等以下09-17描述保留追溯，不代表当前目标与状态。

18:17追加：`rms-shadow`同输入观测及`rms-ablation`分组替换已执行。当前超差复现在隐藏层RMSNorm路径；仅Q/K替换两组输出完全一致，但样本不足以准入。226次旁路调用不是226个模型测试；具体数值与边界见报告§8。

## 目标（一句话）

复用框架已有入口和算子库，明确何时选择优化实现、何时保持同设备原生调用，以及执行失败后不能自动重试的边界。

## 当前阶段与现状

- 总组快速入口：[STATUS.md](../STATUS.md)；开发续接：[HANDOFF.md](../HANDOFF.md)；具体任务与接口草案：[9 月任务规划](../docs/框架接入与安全回退-9月任务规划-20260909.md)。
- 2026-09-16 新增：在锁定推理镜像中完成真实 0.6B 模型的 CPU FP32 对照，2 组输入、8 组模型来源算子用例通过，12 项探针控制测试通过。NPU 模型用例为 0，初始化报 507899，驱动日志为设备命名空间占用冲突。见 [本轮报告](../docs/0.6B模型来源算子基线-20260916.md)。
- 对齐 [910C 阶段目标 v1](../../../docs/运行时层原型验证-战略目标-910C.v1.md) §5「调度」第 3 项：本月交框架接入与多级安全回退方案，不改变推理框架默认行为。训练编排、动态 Batch 不是本方向自动承担的任务。
- 9 月 9 日周会后，下一步围绕 `Qwen/Qwen3-Embedding-0.6B` 梳理实际调用链和代表算子输入，先建立原生基线；额外适配探索单列，不作为统一基座验收成果。
- 设备注册主线采用厂商官方插件，910C 推理腿为 `torch_npu`。锁定训练镜像中的 `torch_fl` 是阶段文件明示的例外；本方向不自行更换它，也不把 Torch-FL 改造作为当前前置任务。

已有验证均有范围限制，不能合称“三级安全回退完成”：

- 910C 历史实验镜像：vLLM dispatch 单测 275 项、真实 NPU 测试 74 项通过；微型随机 Llama 的 4 token 解码与 CPU 对照一致。不是 0.6B Embedding 模型验收。
- PyTorch 独立同步原型：68 项测试通过（8 项控制/CPU、60 项 NPU）；支持作用域内 SiLU、RMSNorm 选择，不是 ATen 全局默认注册，异步互操作问题未闭环。
- 多芯片探索：平头哥原生、FlagGems 直接调用、注册与退出恢复各 8 组通过；壁仞 BF16 原生 4 组通过、FP16 两条路径报错；昆仑芯仅完成 SSH/资源检查，尚无算子结果。

## 目录与代码归属

- `STATUS.md`：当前阶段、量化结果、阻塞、基座差异，供每周收拢。
- `docs/`：任务规划、带日期的实验记录与原始证据；历史记录不替代当前状态。
- `probes/`：本方向统一测试入口、独立接入原型和验证脚本。
- `patches/`：独立子库改动的可追溯补丁，不会自动安装或应用。
- `docker-compose*.yml`、`.env.example`：历史实验配置，保留复现，不能作为本月验收启动配置。

正式算子内核由 FlagGems/厂商库提供，本方向不重复实现。已有 vLLM 改动位于独立仓库 `FlagRT/vllm-plugin-FL` 的 `cgu135/safe-op-fallback` 分支（`635ff6d`）；本次只向 runtime-team 提交方向入口、原型、补丁和证据，不推送该子库，也不将整个子库复制入协调仓。

## 任务看板

| 任务 | 负责人 | 状态 | 依赖 | 出口标准 |
|---|---|---|---|---|
| 既有原型、脚本与证据收拢 | 顾宬 | 已整理，按标准流程合入 dev-1.0 | 个人分支 | 统一入口可访问，历史/当前结果区分清楚 |
| 锁定两套基座的差异核对 | 顾宬 | 推理镜像身份/版本已核对；训练腿未实测 | 总组、device-context | 记录镜像标识、包版本、后端、附加依赖 |
| 0.6B 模型调用链与代表算子基线 | 顾宬 | NPU FP16原生基线及SiLU模型接入通过；隐藏层RMSNorm超差，vLLM本轮未运行 | 推理镜像、模型与可分配设备 | 1 份实际调用与输入记录，2 类候选算子的原生结果或明确不适用原因 |
| 框架接入与安全回退方案 | 顾宬 | 任务/接口草案已整理，待联调完善 | 实际模型路径 | 1 份方案，覆盖选择条件、执行边界、注册恢复与后续验证 |
| 下游接口确认 | 顾宬 | 未确认 | 总组指定对接方 | 至少 1 个下游方向 review 并留记录 |
| 平头哥等额外适配 | 顾宬 | 探索结果保留 | 可用环境和主线进度 | 单独记结果，不冲抵本月 910C 验收 |

## 测试入口

协作流程以根 README「标准提交流程」为准：在个人分支提交，快进同步本地 dev-1.0，将 dev-1.0 合入个人分支处理冲突，再将个人分支 merge 回 dev-1.0 并检查远端无分叉后 push；不走 PR。PR #16 已于 2026-09-17 关闭，main 仍须正式 PR，不直接修改其他子库。

从 runtime-team 根目录运行：

```bash
# 默认仅显示帮助；local 只做语法检查，不导入 torch、不连接服务器、不用卡。
bash dev/framework-adapter/probes/run_checks.sh
bash dev/framework-adapter/probes/run_checks.sh local
bash dev/framework-adapter/probes/run_checks.sh metadata-tests
```

历史 910C 重跑入口（仅在原宿主机、确认资源并人工启动自己的旧容器后执行；不是锁定环境验收）：

```bash
bash dev/framework-adapter/probes/run_checks.sh legacy-pytorch
bash dev/framework-adapter/probes/run_checks.sh legacy-vllm
```

跨芯片探针在已准备好的容器内运行，必须显式选择设备和模式；各模式单独启动进程。镜像、物理/逻辑设备映射和超时命令见 [多芯片记录](../docs/多芯片环境与算子验证-20260909.md)。

```bash
# 示例：平头哥只映射物理设备 1 时，exec 层仍须覆盖 CUDA_VISIBLE_DEVICES=0。
bash dev/framework-adapter/probes/run_checks.sh cross-vendor --device cuda:0 --mode native
```

这些入口不会创建、启动、停止容器或安装依赖。`legacy-*` 只对历史个人容器执行已有测试，不操作其他容器。跨芯片命令必须在对应设备环境执行，不能在本机冒充真机结果。

## 统一基座使用与安全边界

当前基座以 [stack.lock.910c.v2.yaml](../../stack.lock.910c.v2.yaml) 为准，使用总组锁定的训练/推理镜像与容器；v2 登记的新训练镜像仍为候选，不自行切换。需要补装的依赖及版本差异写入 STATUS，供总组收拢；未批准、未验证的依赖升级不进入验收结论。

本轮创建的 `flagos-proto-infer-910c` 标注 `owner=cgu135`，锁定镜像未改，单设备、模型只读挂载，复现参数在 `probes/start_locked_infer_910c.sh`。CPU 对照显式选择 `--device cpu`，不是 NPU 出错后的自动回退；同名容器已存在时启动脚本拒绝覆盖。本轮未安装 FlagGems，镜像内也未查到该发行包。

优先复用 [device-context 原型](../../device-context/prototype/README.md) 的模型与设备入口。现有 `PreferGems` 只在历史 `torch_npu` 环境验证，不可直接套到 `torch_fl` 训练腿；也不向官方 vLLM 推理腿自动安装旧的 FL 补丁。

执行前可因不兼容而选择同设备原生实现；执行异常立即上抛，不承诺自动重试、迁移设备或 CPU 回退。旧 vLLM 原型的 `runtime_fallback_safe` 为兼容既有注册仍默认 True，不能据此推断未审计实现安全；探索测试优先 strict 模式。`resolve()` 返回原始函数，不具备 `call()` / `CachedOp` 的输入检查保障。

## 实验记录

- [2026-09-03 安全回退最小验证](../docs/安全回退最小验证-20260903.md)
- [2026-09-07 910C 单卡联调](../docs/910C单卡联调-20260907.md)
- [2026-09-07 PyTorch 独立接入](../docs/PyTorch独立接入-20260907.md)
- [2026-09-09 多芯片环境与算子验证](../docs/多芯片环境与算子验证-20260909.md)
- [2026-09-16 0.6B 模型来源算子基线与阻塞](../docs/0.6B模型来源算子基线-20260916.md)

---

## 原文件：STATUS.md

# framework-adapter · 当前状态

更新：2026-09-22｜负责人：顾宬 / cgu135

## 当前状态（09-22，覆盖下方历史快照）

- 18:17追加定位：226次真实输入RMSNorm旁路对照均在单算子容差内，30次非逐元素一致；Q/K单独替换两组模型输出完全一致，隐藏层单独替换两组均复现隐藏状态超差。分组实验2/4通过，不是RMSNorm全部准入；默认仍保留原生。见报告§8，容器已停止，证据已下载。未修内核或放宽阈值。

- 27本人锁定推理容器已恢复NPU执行；Qwen3-Embedding-0.6B FP16原生2组模型输入、8组模型来源用例通过。旧aclInit阻塞本轮未复现，未认定唯一根因。
- 新增真实Qwen3模块作用域适配，解决自定义RMSNorm绕过F.rms_norm的问题；只对指定模型实例生效，退出恢复。
- 最终15项控制测试、8组目标算子对照、4类NPU回退检查通过。2组输入×关闭/仅SiLU接入=4组模型配置通过，隐藏状态和Embedding与原生完全一致。
- RMSNorm已经命中FlagGems，但模型隐藏状态误差超出既有诊断阈值；只替换RMSNorm也复现。默认不准入，保留原生；不放宽阈值。Embedding向量对照通过不抵消隐藏状态失败。
- 版本：FlagGems f7ae8e6b934a33ec1ccaf2c9aae71edf205f8fb4源码挂载，SQLAlchemy2.0.48/greenlet3.2.5安装在本人目录；torch2.10/torch_npu2.10、Triton3.5.0/triton-ascend3.2.1沿用锁定推理镜像。未切FlagTree构建物，不宣称FlagTree适配完成。
- 源码默认allowlist为空；demo显式启用SiLU；RMSNorm模型替换必须显式实验开关。训练、vLLM融合、多流、BF16及芯片级恢复未在本轮验证。
- 09-21会议后任务已从旧“只出方案”推进为NPU开关和三级回退可行性验证、算子组依赖清单，并协助vLLM/Megatron接入。当前只完成局部原型，不是三级回退总验收。
- 27容器已停止；11能SSH但Docker权限不足，未修改其权限。允许资源范围按用户最新通知为27/11。未推送或修改共享文件。

详情、复现及失败证据：[09-22模型接入与回退报告](../docs/910C模型接入与安全回退-20260922.md)。下一步对齐RMSNorm语义/误差要求及算子注册清单，再扩模型/引擎范围。

## 09-17历史快照（以下保留追溯，不代表当前状态）

### 当时阶段

对应 [910C 阶段目标 v1](../../../docs/运行时层原型验证-战略目标-910C.v1.md) §5「调度」第 3 项：框架接入与多级安全回退方案，支撑单卡推理；本月不改变推理框架默认行为。目前完成模型来源 CPU 对照，NPU 初始化阻塞，尚未完成锁定环境 NPU 模型验收。

## 本周新增结果

- 在 27 的锁定推理镜像运行真实 Qwen3-Embedding-0.6B：CPU FP32 的 2 组模型输入、8 组代表算子用例通过（6 RMSNorm、2 SiLU）；新增探针的 12 项无硬件控制测试通过。重复运行不累加。
- 记录真实输入规格、原生数值对照、输入/权重不变及原生重放。仅为诊断 eager 前向，不是服务性能、检索质量、优化实现或回退验收。
- 明确接入缺口：Qwen3RMSNorm 自行展开归一化，不经过历史原型拦截的 F.rms_norm；vLLM 融合路径目前仅核对源码，未运行引擎。
- NPU FP16 最终停在初始化，报 aclInit 507899 / Resource_Busy；本轮 NPU 模型/算子通过数为 0。驱动同期记录设备 0 命名空间占用冲突，不等同于显存满，也不能仅据此认定容器数量上限。
- 脚本、环境/模型哈希、结果与失败日志已保存，本人容器已停止。见 [本轮报告](../docs/0.6B模型来源算子基线-20260916.md)、[开发续接](../HANDOFF.md)。

## 历史结果（不算本周新增）

- 910C 历史实验：PyTorch 同步接入原型 68 项测试通过；vLLM dispatch 275 项单测、74 项真实 NPU 测试通过。计数为各自测试项，重复轮次不累加。
- 平头哥：SiLU/RMSNorm 的原生调用、FlagGems 调用、注册与退出恢复各 8 组通过，共 24 组。
- 壁仞：BF16 原生测试 4 组通过；FP16 的 SiLU、RMSNorm 均在首个输入报错。昆仑芯：SSH 可用，但无 Docker/sudo 使用权限，未执行算子。
- 已将方向入口、已有脚本和证据整理到本目录，并补充 [具体任务及接口草案](../docs/框架接入与安全回退-9月任务规划-20260909.md)。详见 [README](../README.md) 和日期记录。

以上都是原型/探索结果，不代表已完成本月两套锁定环境、0.6B 模型或三级回退验收。

## 基座差异

- 9 月 7 日测试使用 `leinao/flaggems-cann9.0.0:v1.0`、个人容器 `flagos-cgu135-dev-910c`，不是锁定基座；torch/torch_npu 2.10、Triton 3.5.1、FlagGems 5.3.5，另装了个人修改的 vllm-plugin-FL。
- 当前锁为 [stack.lock.910c.v2.yaml](../../stack.lock.910c.v2.yaml)。推理镜像 quay.io/ascend/vllm-ascend:v0.20.2rc1-a3 的本地 digest 与锁文件一致；推理腿采用厂商 torch_npu，训练腿 torch_fl 为例外，v2 新训练镜像仍为候选，本轮未验证或切换训练腿。
- 平头哥/壁仞使用各自已有厂商镜像，属于额外探索；各个人测试容器在当日测试结束时已停止，未删除。当前资源占用需运行前重新检查。
- 本轮使用 27 新建的本人容器 flagos-proto-infer-910c，仅映射设备 0，模型只读挂载，结束已停止。未安装依赖、改他人容器或装入旧 FL 补丁；锁定镜像未查到 flag-gems 发行包。
- 9 月 16 日实验代码基线为 f4d0ddd；9 月 17 日已抓取公共分支 c2dd4d2。PR #16 已关闭、未合并；按根 README 的标准流程在个人分支提交后，经本地 merge 同步 dev-1.0，不再走 PR，不直接在共享分支开发。

## 下一步（拟在下次周会前推进）

1. 按 9 月 16 日周会要求，整理算子接入与回退需求，向算子组确认清单、别名/实现对应关系、接口与支持条件；对应关系不等于语义等价，未知实现不自动替换。
2. 协调可分配设备或明确允许共用的推理容器，在锁定镜像补齐同一探针的 NPU FP16/BF16 原生基线，不以 CPU 结果抵替。
3. 观测真实 vLLM Embedding 路径，明确 RMSNorm（含 residual）和 SiLU-and-Mul 融合入口；源码核对不能代替运行记录。
4. 根据上游接口确认和实际调用链完善接入及回退方案、测试矩阵；请至少 1 个下游方向确认，确认前标记为草案。不自行承担通用自定义算子识别或内核优化。

## 阻塞与需要协调的事项

- 当前阻塞为设备访问冲突，尚未识别占用者或确认唯一根因；不擅自进入他人环境、停任务或绕过隔离。历史 68/74 项结果不能移作本轮验收。
- 请总组确认本方向的下游 review 对接人，以及“本月出方案、不改默认行为”对应的 demo/合入证据要求；当前未满足下游确认及本方向锁定环境验证条件。
- 训练腿的依赖与候选镜像状态跟随最新公共锁文件、总组/device-context 收拢，本方向不自行拍板。
- 额外探索：昆仑芯环境权限、壁仞 FP16 错误待处理；不作为 910C 主线阻塞。

---

## 原文件：HANDOFF.md

# framework-adapter · 开发续接

更新：2026-09-22。这是可重读的工程记录，不代替实时资源检查。先读 [STATUS](../STATUS.md)，再看日期证据；不要从聊天摘要猜环境、测试通过情况或 Git 状态。

## 09-22最新执行检查点

- 18:17后续：新增qwen_rms_shadow.py（hash895b0e92…）及qwen_rms_ablation.py（e71ef92b…），server /work同名；run_checks增加rms-shadow/rms-ablation。226次旁路单算子均满足诊断容差、30次不完全一致；分组Q/K56调用×2组模型完全一致，隐藏层57调用×2组复现原隐藏状态超差，最终向量仍在阈值内。FP32/拆乘权重未消除差异，具体指令根因未查明，不改默认准入。
- 新结果rms-*-20260922位于原宿主results及本地方向证据目录，含源码hash、15项控制回归、停止记录。诊断脚本completed/exit0仅指收集成功，不能当全部数值通过。18:17:35 running=false，SSH已退出；未commit/push/OneDrive写入。下一步依据报告§8同算子/精度方向对齐，勿盲目调容差或扩大准入。

- 已在27本人flagos-proto-infer-910c验证NPU原生及FlagGems真实模型路径，结束停止。允许资源范围27/11；11尚无Docker权限。本轮旧507899未复现，不继续沿用“当前初始化阻塞”。
- 代码新增qwen_scoped_adapter.py、test_qwen_scoped_adapter.py、qwen_gems_validation.py；run_checks新增qwen-controls/qwen-gems。当前本地个人分支b3dd648工作区改动，未commit/push，未碰用户.DS_Store。
- fetch最新origin/dev-1.0=3d3d598，未merge；v2推理锁未变。服务器runtime初始化仍用0916保存副本，不能说所有代码已同步最新公共版本。
- FlagGems源码在宿主acceptance-20260916/FlagGems-f7ae8e6-20260922；依赖在同目录deps-20260922，容器对应/work下；未全局安装或修改镜像。宿主git有失效代理，本轮仅对下载命令临时清理代理环境及http.proxy配置，没有改全局配置。
- 最终15控制测试、8目标算子对照、4回退场景通过；2组模型输入关闭/SiLU-only四配置通过。RMSNorm目标路径命中但隐藏状态对照失败，保留原生，未放宽阈值。不要只报总pass或只报“RMSNorm拦不到”。
- 服务器最终runner为/work/qwen_gems_validation-v3.py，与本地qwen_gems_validation.py哈希一致；旧v1/v2及结果保留，失败源码有tgz快照。结果文件均以20260922命名，已下载本方向docs/evidence-model-20260922。
- 详见[09-22报告](../docs/910C模型接入与安全回退-20260922.md)。下一步优先RMSNorm语义/数值对齐、算子清单和兼容约定，后续vLLM/Megatron另测；非默认启用RMSNorm，非性能/训练/跨芯片恢复验收。

下方旧日期环境/阻塞作为历史记录，不覆盖上述结果。

## 固定约定

- 负责人顾宬 / cgu135；个人分支 `cgu135/framework-adapter`。9 月 17 日按用户确认及根 README 改为：个人分支 commit，快进同步本地 dev-1.0，dev 合入个人分支解决冲突，个人分支 merge 回 dev 后检查无分叉并 push。不走 PR，不直接在共享分支开发、不 rebase/强推共享分支；main 仍须正式 PR。
- runtime-team 收拢方向入口、原型、探针和证据。已有 FL 子库改动在独立 `vllm-plugin-FL` 分支 `cgu135/safe-op-fallback`，不能假定主仓提交会同步子库，也不把它自动装入官方镜像。
- 周报使用本周新增数字，区分 CPU/NPU、单算子/模型、原型/验收；重复轮次不累加。不写凭证，不擅自同步 OneDrive。
- 不停他人容器、不抢卡、不修改宿主驱动或规避设备隔离。是否有空闲计算和是否获得设备访问是两回事。

## 当前事实与入口

- 公共基线：`origin/dev-1.0@f4d0ddd`，已合入个人本地分支。锁文件是 [v2](../../stack.lock.910c.v2.yaml)，训练候选镜像未切换；本轮只用锁定推理镜像。
- PR [#16](https://github.com/FlagRT/runtime-team/pull/16)：2026-09-17 查询 CLOSED、未合并；用户明确要求依根 README 直接 merge/push 到 dev-1.0。本次整理包含 9 月 16 日新结果，提交状态以 Git 实查为准。
- 27：`cgu135@10.120.72.27`，连接凭证不在仓库。用户确认可用；运行前仍要重查资源和权限。
- 本人容器 `flagos-proto-infer-910c`，ID 前缀 `7a13465c20ee`、label `owner=cgu135`；本轮创建，仅映射 davinci0。结果完成后停止，重用前核对归属和配置。
- 宿主持久目录：`/home/cgu135/framework-adapter-910c/acceptance-20260916` → 容器 `/work`。模型 `/mnt/raid/hliu553/models/Qwen3-Embedding-0.6B` → `/model:ro`。
- 镜像 ID `2e56022ae5b3…`，digest、模型 SHA256、包版本、脚本 SHA256 均在 [证据目录](../docs/evidence-model-20260916/)。不能只对 tag。
- 新入口：`probes/qwen_embedding_baseline.py`；服务器最终执行快照 `/work/qwen_embedding_baseline-v3.py`。本地最终脚本需与 `probe-v3.sha256` 相符。前两版保留在宿主目录，不覆盖旧日志。
- 9 月 16 日收尾已下载最终日志，确认本地/远端 v3 脚本哈希一致、12 项控制测试与语法检查通过，见 [本地核验](../docs/evidence-model-20260916/local-verification.md)。当日未提交/推送；9 月 17 日按新流程同步，仍无 OneDrive 写入。

## 本轮结果及边界

- CPU FP32：真实模型 2 组输入通过，8 组代表算子用例通过（6 RMSNorm、2 SiLU）；12 项无硬件控制测试通过。
- NPU FP16：最终 v3 在设备初始化报 `aclInit 507899 / Resource_Busy`，device_count=0；模型/算子 NPU 通过数为 0。内核日志 `Conflict open udevid` 指向设备 0 的命名空间占用冲突，未确认拥有者或唯一根因。
- 关键接入发现：Transformers `Qwen3RMSNorm` 自己展开归一化，不经过 `F.rms_norm`，历史拦截原型不能直接覆盖它。vLLM 融合入口只做了源码核对，未运行引擎，不可混称已观测调用。
- 所有 CPU 结果为诊断 eager、小输入、前向，不是模型检索质量/服务/性能/训练验收，也不是优化算子或回退验证。

## 下一步顺序

2026-09-16 19:08 只读复查：宿主 npu-smi 未列计算进程；运行中的 flagos-proto-train-910c 和 flaggems-cann9.0.0 均映射全部 16 个 davinci 设备节点（后者 privileged）。这只是映射/运行快照，不证明具体占用者；本次没有重跑 aclInit。本人推理容器仍 exited，未修改远端状态，SSH 已退出。

19:22 再查：rag-ljy-vllm-910c 已运行，带卡容器总数达到 3（另外两个同上）；设备 2/3 有 Python 计算，设备 0 未列进程。按锁文件并发规则，没有启动本人的第 4 个带卡容器，也没有执行初始化测试。因此没有新增 aclInit 错误/恢复结论。本人容器保持 exited，SSH 已退出，待协调串行窗口或允许共用的环境。

1. 优先整理接入与回退需求，由组内向算子组确认算子清单、对应关系、调用契约和支持条件；先确认已有机制再写必要适配，不直接扩展为自动识别所有自定义算子。
2. 协调可分配的设备，或明确被允许共用的公共推理容器。不能自行进入他人的运行环境执行探针；先确认配置/设备分配，再恢复测试。
3. 在锁定镜像运行同一探针的 NPU FP16/BF16 模式，保留新输出文件名；不修改容差以掩盖错误。
4. 补充真实 vLLM Embedding 路径观测，分别核对融合 RMSNorm（含 residual）与 SiLU-and-Mul；CPU 的普通模块结果不能替代。
5. 用实际输入与调用链修订接入方案，确认模块入口/注册入口、支持条件与执行后上抛边界；请求至少 1 个下游 review。
6. 更新 STATUS、日期报告和本文件，再按用户授权及标准 merge 流程提交/推送。源码和证据存在宿主及本地，不只留在容器可写层。
