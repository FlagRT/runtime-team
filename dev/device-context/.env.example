# runtime 子方向专属环境变量模板（cp 成 .env 按需调整）
# 公共环境变量已由 compose.base.yml 注入，无需重复：
#   GEMS_VENDOR=ascend
#   TRITON_ENABLE_TASKQUEUE=false
#   FLAGCX_PATH=/workspace/FlagCX/plugin/torch
#   HCCL_NPU_SOCKET_PORT_RANGE=16666,16676

# 容器内 export（当前 compose 未注入，进容器后手动设）
DO_NOT_TRACK=1
