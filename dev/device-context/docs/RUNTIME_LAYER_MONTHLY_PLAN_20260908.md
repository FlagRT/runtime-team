# 运行时层 · 9 月工作计划（本月聚焦版）
## —— v2.0：统一基座 × 本月目标 × 逐周任务分解（2026-09-08）

> **按评审意见改版**：本版不再是全周期项目规划，收敛为三件事——
> **① 现阶段全组的最小统一基座（§1）② 本月统一目标（§2）③ 各子方向接下来几周的具体任务（§3）**。
> 每周按 §4 机制做进度反馈对齐。**后续月份只保留大方向（§5），到时再细化。**
>
> **方向关键词**（全文统一）：**设备**（设备抽象与执行上下文，⭐我们）｜**算子**（数据流接入与算子调用适配）｜**调度**（任务调度与执行引擎）｜**显存**（显存与缓存管理）｜**分布式**（分布式训练与推理支撑）｜**监控**（监控诊断与可靠性）｜**精度**（精度与性能分析）｜**总组**（协调层）。
> 与战略文档方向名的对应：设备=device-context｜分布式=communication｜显存=memory｜精度=performance｜监控=监控可靠性。

---

## 1. 现阶段最小统一基座（全组共同，归拢自战略文档）

所有子方向开工前自查以下基座；**结论性验证只在基座内做，日常调试环境的结果不作为验收依据。**

### 1.1 锁定镜像（写入 `dev/stack.lock.910c.yaml`，任何改动知会全组）

| 角色 | 镜像 | 覆盖场景 | 容器命名 |
|---|---|---|---|
| 训练腿 | `flagrt/ascend-operator-runtime-comm:0.1.3-cann9.0-py311-torch2.10-flagcx0.13.0g55eb2ffp2-arm64`（容器内补装 `transformers`） | 2 卡分布式微调；通信库与算子库已内置 | `flagos-proto-train-910c` |
| 推理腿 | `quay.io/ascend/vllm-ascend:v0.20.2rc1-a3` | 单卡推理服务 | `flagos-proto-infer-910c` |

### 1.2 运行时统一 API 与可复用组件（设备方向已交付 ✅，全组下游使用）

| 资产 | 位置 | 状态 |
|---|---|---|
| 统一运行时 API + Backend 注册表 + 昇腾后端 | `runtime/`（`use/set_device/create_stream/translate_error/recover_device`） | ✅ 真机 37/37 |
| conformance 基线（13 例 + 6 例，统一 API 跑通） | `runtime/conformance/` | ✅ 13/13 + 6/6 |
| 错误码翻译（108 条映射 + F5 可观测） | `runtime/backends/ascend`（复用 conformance/errors.py） | ✅ |
| 状态恢复（四态 + probe/real/hybrid 重建） | 复用 recovery.py + device_state.py | ✅ |
| 组件 v0.1（打包下发，去 torch_fl 历史命名） | 待打包 🔲 | 本周 |

**下游使用方式**：`runtime.use("ascend")` 一行接入；问题反馈给设备方向，按周迭代组件版本。

### 1.3 合入把关（全部子方向的合入门槛，五条同时满足即合入）

① 镜像与 stack.lock 一致 ② 容器/分支命名合规 ③ 自己负责的接口约定章节已写完且至少一个下游方向确认可用 ④ 能跑通自己负责的那段 demo ⑤ 未引入新镜像或环境分叉。

---

## 2. 本月统一目标（2026.09）

> **在 910C 指定版本锁定镜像上，跑通"2 卡分布式微调 + 单卡推理服务"两段式 demo，验证运行时层核心能力（设备执行上下文管理、多卡通信、显存管理、监控与故障恢复）真实可用，并产出统一验收报告。**
> 验收模型：`Qwen/Qwen3-Embedding-0.6B`。

**验收标准（全部满足 = 本月达成）**：

| # | 标准 | 当前状态 |
|---|---|---|
| 1 | 段一：2 卡分布式微调在**锁定训练镜像**内跑通，loss 正常下降，无死锁无数据错乱，集合通信正确性有对照证据 | 🔲（设备侧能力已验证，待在锁定镜像内复跑） |
| 2 | 段二：单卡推理服务在**锁定推理镜像**内正常启动，向量结果正确，有吞吐/时延数据 | 🔲（此前 serve 验证在同类镜像，待按锁定版本复跑） |
| 3 | 两段验证中至少注入一次真实错误，观察到"统一错误识别 → 恢复处理"完整闭环 | 🔲（错误翻译+恢复能力已备，待编入 demo 流程） |
| 4 | 显存方向产出锁定环境显存池定义 + 峰值画像（分层缓存不在本次硬性指标内） | 🔲 |
| 5 | 精度（评测）方向产出基于两段 demo 实际证据的**统一验收报告** | 🔲 |
| 6 | 全程只用两个锁定镜像，不引入第三方临时镜像/容器 | 持续约束 |

---

## 3. 9 月逐周任务分解（各子方向）

> 状态：✅ 完成｜🔄 进行中｜🔲 待做。每周五下班前各方向按 §4 格式反馈进度。

### W1（9/1–9/4）基座对齐 —— 已过

| 子方向 | 任务 | 状态 |
|---|---|---|
| **设备** | 统一 API + 昇腾后端 + conformance 13/13+6/6 + demo 跑通；接口约定文档初稿 | ✅ |
| **总组/全组** | 战略文档定稿；stack.lock 落地；各方向就共同任务范围对齐 | 🔲 总组牵头 |

### W2（9/7–9/11）本周：组件下发 + 单腿打通启动

| 子方向 | 本周任务 | 交付物 |
|---|---|---|
| **设备** | ① 组件打包（三大句柄+流/事件+错误+状态+恢复）去 torch_fl 命名 → **v0.1 下发** ② 接口约定文档（设备上下文章节）定稿 ③ 错误注入→分级→恢复闭环演示脚本备好 ④ 训练脚本（Qwen3-Embedding-0.6B 2 卡微调）在**锁定训练镜像**内预跑 | 组件 v0.1、接口约定章节、闭环演示脚本、预跑记录 |
| **算子** | 历史材料按目录规范收编提交（本月不新增实现） | 收编提交 |
| **调度** | 历史材料按目录规范收编提交（同上） | 收编提交 |
| **显存** | 在**锁定推理镜像**上启动显存画像（旧结论复核）；显存池定义初稿 | 画像进行中记录、池定义初稿 |
| **分布式** | 与设备共同 2 卡微调：通信正确性对照方案（all_reduce/all_gather/P2P）定稿；历史通信缺陷整理为干净提交 | 对照方案、干净提交 |
| **监控** | 错误注入→恢复闭环的监控侧证据方案；指标基线首版（设备/内存/流三类） | 证据方案、指标基线 |
| **精度** | 两个锁定镜像发布到团队仓库并回写 stack.lock；基础规格评测用例跑通；性能采集口径定义（对齐预热/并发） | 镜像标识记录、评测结果、口径文档 |

### W3（9/14–9/18）单腿打通完成 + 接口约定定稿

| 子方向 | 本周任务 | 交付物 |
|---|---|---|
| **设备** | 段一底座部分完成（微调在锁定镜像内跑通、设备侧证据）；段二设备侧支撑（推理腿上下文/流/恢复证据）；组件 v0.1.x（吸收反馈） | 训练腿底座证据、推理腿设备证据、组件 v0.1.x |
| **算子** | 收编材料查漏补缺 | 收编完成确认 |
| **调度** | 收编材料查漏补缺 | 收编完成确认 |
| **显存** | 显存画像完成 + 显存池定义与对照数据定稿 | 画像报告、池定义+对照数据 |
| **分布式** | 段一通信部分完成：2 卡微调通信正确性对照证据；通信接口约定文档定稿 | 通信对照记录、接口约定章节 |
| **监控** | 闭环监控证据采集完成（配合设备的注入演示）；指标基线首版交付 | 闭环监控证据、指标基线 |
| **精度** | 两段 demo 性能数据采集（训练吞吐 + 推理吞吐/时延，口径对齐）；向量结果正确性校验方案就绪 | 性能数据、校验方案 |

### W4（9/21–9/25）联调 + 验收 + 合稿

| 子方向 | 本周任务 | 交付物 |
|---|---|---|
| **设备** | 错误注入→统一识别→恢复闭环在两段 demo 中真实走通（验收标准 3）；遗留问题收口 | 闭环证据 |
| **算子** | —（本月不新增实现） | — |
| **调度** | —（本月不新增实现） | — |
| **显存** | 显存侧证据并入验收报告（验收标准 4） | 画像+池定义终稿 |
| **分布式** | 段一联合收口（与设备互验）；通信库干净改动提交合入（过 §1.3 把关） | 段一完整证据、合入提交 |
| **监控** | 故障恢复链路在验收流程中的证据固化 | 恢复证据链 |
| **精度** | **统一验收报告**（汇总全方向证据 + 性能数据，验收标准 5）；向量正确性校验执行 | **统一验收报告** |

### W5（9/28–9/30）缓冲与收尾

各方向处理验收遗留项；组件 v0.1 反馈汇总进入 10 月组件 v0.2 排期；**下月计划按 §5 大方向细化后发布**。

---

## 4. 每周进度反馈对齐机制

```
每周五 17:00 前  各子方向在周报线程反馈（固定三行格式）：
                ① 本周完成（对照 §3 周表逐项 ✅/🔲 + 证据链接）
                ② 下周计划（对照 §3 下周表）
                ③ 阻塞项（需要谁配合、需要什么资源）
每周五 17:30    周会对齐（15 分钟）：逐方向过 ①②③；阻塞项当场指定解决责任人与时限
跨方向问题      当场按协同机制路由（见附录）：定位方出证据 → 修复方认领 → 下周会首查闭环
```

**原则：让每个人清楚自己本周干什么、下周干什么，而不是各自解读各自摸索。**

---

## 5. 后续月份大方向（只定方向，到时再细化）

| 月份 | 大方向（对外考核） |
|---|---|
| 2026.10 | ≥20 模型单芯片推理验证；组件 v0.2 迭代；问题路由机制开始运转 |
| 2026.11 | ≥2 家新芯片接入（昆仑芯 + 寒武纪，单个 ≤5 人天）；接入手册 |
| 2026.12 | 300 模型 × 3 芯片（精度差 ≤5%）；多级恢复体系落地；反馈高峰响应 |
| 2027.01 | 典型检索模型性能提升 ≥20%（Profiling 定位驱动） |
| 2027.02 | ≥2 类向量检索组件协同验证（流水编排） |
| 2027.03 | 训练框架适配样例 + 8 卡规模验证启动 |
| 2027.04 | ≥2 类训练框架端到端验证（规模化主战场） |
| 2027.05 | 离线特征与训练链路调通 |
| 2027.06 | ≥500 模型 + ≥3 家芯片；接口规范 v1.0；半年合稿 |
| 2027.07-12 | 全量适配、调优、总体验收 |

> 每月初按当月大方向 + 基座现状，产出当月版"统一目标 + 逐周任务分解"（即本档 §2–§4 的月度刷新），上月机制与模板直接复用。

---

## 附录：长期协同机制（精简版，长期有效）

**问题路由**（谁定位、谁修）：
- **精度差距 / 性能不达标**：精度方向用 Oracle-Device 回归 / Profiling 五段分解（算子/同步/显存/调度/通信）定位 → 算子差异归**算子**（涉及实现升级上层算子层联调）、设备因素归**设备**、显存归**显存**、调度归**调度**、通信归**分布式**；修复后由精度复测闭环。
- **故障**：监控检测 + 设备错误分级（L1 重试 / L2 上抛 / L3 重放+重调度 / L4 设备恢复+检查点）。
- **规模化通信异常**：分布式在 8 卡/多机检测 → 底座问题回流设备、并行策略分布式自优、厂商栈缺陷分布式跟进。

**规模递进**：设备只负责最小正确规模（≤2 卡）；8 卡→多机上量归分布式；高并发归调度；规模化观测归监控/精度。避免各方向挤在 2 卡上重复验证。

**升级路径**：子方向内 → 跨方向三方会诊 → 总组裁决 → 运行时层外（算子问题经算子方向、并行问题经分布式方向，与上层算子层/编译层联调）。

**固定联调对**（每月至少一次同步）：设备↔分布式（规模化）、算子↔精度（精度定位）、调度↔分布式（调度执行）、显存↔调度（显存调度）、监控↔精度（指标口径）、设备↔监控（恢复原语）。
