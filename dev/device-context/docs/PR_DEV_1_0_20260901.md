# PR 描述：同构 910C 设备执行上下文职责验收（训练达标 + 推理四阶段闭环）

> 目标分支：`dev-1.0` ｜ 源分支：`kistich/device-context`（11 commits，33 files，+3607）
> 作者：Kistich ｜ 日期：2026-09-01

---

## 概述

把「设备执行上下文」职责——**统一设备/内存/执行句柄及生命周期、Stream 语义、Host/Device 异步传输、页锁定内存与双缓冲流水线、同步语义、错误码翻译、设备状态恢复**——在**同构 910C** 环境下，从「训练侧已达标」推进到「推理侧四阶段（P0-P3）全验收闭环」。

一句话结论：**11 项职责（D1-D11）在训练与推理两个场景全部验证通过**，并产出两个可直接复用的结论——TP 数值等价对照纪律、ACL 107015 根因定位。

---

## 交付清单

| 方向 | 交付物 | 状态 |
|---|---|---|
| 推理 P0 | dense 单卡离线推理（vllm-ascend 官方镜像） | ✅ `DENSE_INFER_PASS` |
| 推理 P1 | conformance 推理版 6/6 + 双缓冲流水线真实现 | ✅ 6/6 PASS |
| 推理 P2 | Qwen3-4B TP=1/2/4 输出对照 | ✅ greedy 下**短前缀一致、长序列渐进分叉，语义等价**（2026-09-02 修正，见 §关键发现 1） |
| 推理 P3 | 服务化 × 子进程句柄 / 错误码翻译 / 状态恢复 | ✅ 8/8 + 翻译链路验证 |
| 训练 | 职责 × 分布式训练映射与验收评估 | ✅ 11 项达标 |
| 文档 | 职责映射（推理/训练）+ 四份执行记录 + 方案修正 | ✅ 已归档 |

---

## 验收结果

### 推理侧（A1-A11）

| # | 验收项 | 结果 |
|---|---|---|
| A1 | dense 单卡离线推理 | ✅ 同口径（16×256 批量）重测 **283.0 tok/s（Qwen2.5-1.5B）/ 285.2 tok/s（Qwen3-4B）**；原记 19.5 tok/s 系预热不足污染，已作废 |
| A2 | conformance 推理版 | ✅ 6/6 PASS |
| A3/A4 | 双缓冲机制 + 重叠深挖 | ✅ 多流真并发（kernel 时间线证实）；瓶颈 = EVENT_WAIT 3.37ms/12 次 |
| A5 | TP=2 与 TP=1 输出对照 | ⚠️ **结论修正**：原记「greedy 逐字一致 4/4」样本仅 4×64 token 不足。16×256 复测为**数值不等价（4/16 全长一致，一致 token 50.1%，一致前缀中位 ~80 token）但语义等价**，属自回归固有特性非缺陷 |
| A6/A7 | TP 无 NaN（同步语义）+ 流绑定 | ✅ `TP_COMM_PASS 4/4` |
| A8 | EngineCore 子进程设备句柄 | ✅ 持有 **7 个 davinci fd**（原记「1」为去重路径数）+ 设备映射 **davinci 78 + CANN 库 524 + 文件 3010**（原记「124」为过滤子集）；功能请求 0.23s；停止/SIGKILL 后均**无残留** |
| A9 | 推理路径错误码翻译 | ✅ `ACL_107015_PASS`（真实错误注入 + A/B 对照） |
| A10 | 四态监控 + 五段式恢复 | ✅ `DEVICE_STATE_RECOVERY_PASS 8/8`，含 L4 完整 R1→R5 |
| A11 | 结果与 trap 归档 | ✅ 四份执行记录 |

### 职责覆盖（D1-D11）

D1 运行时封装 · D2 设备句柄 · D3 内存句柄 · D4 执行句柄 · D5 Stream 语义 · D6 异步传输 · D7 页锁定内存 · D8 双缓冲流水线 · D9 同步语义 · D10 错误码翻译 · D11 设备状态恢复 —— **11/11 在训练与推理场景均有验证结论**。

---

## 关键发现（可复用）

### 1. TP 数值等价对照必须用 greedy（纪律级）

随机采样（temperature=1.0）下 TP=1/2 输出**必然发散**——TP=2 的 HCCL all_reduce 浮点累加顺序差异被随机采样放大，实测 0/4 一致（但语义均正常）。改用 greedy 后一致性显著提升，但**并非逐字一致**。

**2026-09-02 修正（重要）**：16 prompt × 256 token 严谨复测显示，greedy 下 TP=1 与 TP=2
**数值层面不等价**（4/16 全长一致，一致 token 50.1%，一致前缀中位 ~80 token），
但**语义层面等价**——分叉后输出均合理完整（如 fibonacci 列表版 vs 标量版）。
分叉早晚与任务选择空间负相关（质数表/数数等确定性任务全长一致，开放式大纲从第 0 token 就分）。

> 影响（已更新）：**判定缺陷应看「随机性 / NaN / 乱码 / 语义崩坏」，
> 而不是「与 TP=1 逐字不同」**。联调压测时若看到 TP 分叉，按原结论会误判为同步缺陷。

### 2. ACL 107015 根因定位（stream callback 契约）

```
rt_error_codes.h:36  ACL_ERROR_RT_STREAM_NO_CB_REG 107015 // callback not register to stream
```

单变量 A/B 对照（唯一变量 = 是否 `subscribe_report`）：

| 组 | 操作 | 返回 |
|---|---|---|
| A | `create_stream` → 直接 `launch_callback` | **107015** |
| B | `create_stream` → `subscribe_report` → `launch_callback` | **0（成功）** |

**结论：对未注册 callback 的 stream 投递 callback 即命中——调用方契约违反，非设备缺陷。**

### 3. 错误码映射表优先级高于消息语义

构造 `"device lost, error code is 507021"` 期望判 L4_FATAL，实际因 507021 已映射为 L1_RESOURCE，消息关键词**未生效**。
→ 验证 L4 类行为时，须使用**不带已知错误码**的纯消息。

### 4. 双缓冲瓶颈定位（Level1 kernel 时间线）

昇腾多流**真并发**成立（copy 与 matmul 重叠可观测），推翻「并发能力受限」推测；真实瓶颈是 `EVENT_WAIT`（3.37ms / 12 次，单次最高 391us ≈ 39 个 matmul）——**优化方向是减少同步点，而非提升并发**。

### 5. 环境结论：A 线走 vllm-ascend 官方镜像

- `VLLM_PLUGINS=fl` 会破坏 platform 选择 → `RuntimeError: Device string must not be empty`（vllm-plugin-FL 是 torch_fl 原生栈插件，与 torch_npu 冲突）。
- 昇腾平台由 `quay.io/ascend/vllm-ascend:v0.20.2rc1-a3` 镜像内置提供，vllm-plugin-FL 路线在同构 A 线不适用。

---

## 同步语义验证结论（FlagCX 相关）

| 项 | 结论 |
|---|---|
| TP 通信流绑定 | ✅ collective 与当前流绑定正确，all_reduce 后设备侧消费正确 |
| 异步 all_reduce 后数据可见性 | ✅ `TP_COMM_PASS 4/4`，未复现「异步无同步导致 NaN」类问题 |
| 注入错误后的状态恢复 | ✅ L4 完整走通 `captured → evaluated(isolated) → recovered → replay_ready` |

> 说明：`recovery.handle_error` 仅对 L4_FATAL 触发 R2-R5 设备恢复，L1-L3 仅标记 replayable——这是 R2「避免不必要高代价重建」的设计意图，已在 P3 验证中确认。

---

## 复现方法

```bash
docker exec -it flagos-infer-910c bash
cd /mnt/raid/hliu553/runtime-team/dev/device-context/benchmarks/inference

# O2 服务化
bash start_vllm_serve_910c.sh 8100
python3 probe_serve_health.py --port 8100 --wait 1800

# A8 子进程设备句柄
python3 probe_enginecore_device_ctx.py --port 8100

# A9 107015 注入
python3 probe_acl_107015.py --device 0

# A10 四态 + 恢复
python3 probe_serve_state_recovery.py --port 8100 --ordinal 0

# TP 数值等价（greedy）
bash run_qwen3_tp_compare.sh 2 greedy
```

---

## 后续

1. 107015 分级建议由 `L3_EXECUTION` 调整为 `L2_PARAM`（契约违反应上抛调用方，L3 的重放必然再失败）
2. 双缓冲「减少同步点」优化（EVENT_WAIT 瓶颈）
3. 与同事昇腾线（Qwen3-4B 基准）对齐后的联合压测

---

## 2026-09-02 补充：集成落地 + P1/P2 收尾（本次 PR 新增内容）

> 上一版（09-01）完成"验证层面"验收；本轮把职责从**模块验证**推进到**集成可用**，
> 并补齐此前遗漏的子项。以下为本次 PR 相对 09-01 的新增内容。

### 1. D10/D11 集成到真实推理服务（从"模块验证"到"职责落地"）

| 组件 | 做法 | 效果 |
|---|---|---|
| `inject_error_translation.py` | `python -c` 包装器在 `cli.main()` 前替换 vLLM 的 4 个异常 handler（**不修改 vLLM 源码**） | 所有未捕获异常先经 `translate_error` 再走原逻辑 |
| `device_state_monitor.py` | 独立进程旁路监控 | 周期探活 + 四态查询，状态变化落事件 |
| `start_vllm_serve_910c.sh` | 注入式启动 + 并行监控 | 一条命令拉起整套 |

**实测**：超长 prompt（5000 token）→ HTTP 400 + 日志
`[device-context][exception_handler][L2_PARAM] :: VLLMValidationError...`

**集成暴露的真实分级缺陷（已修复）**：`VLLMValidationError` 此前被关键词兜底判为
`L3_EXECUTION`（重放无意义），新增规则后正确判为 **L2_PARAM**。
**集成还暴露 3 个仅集成可见的 bug**（handler 必须 async def、外层 wrap 不能 async、pkill 自杀）。

### 2. D8 双缓冲落地（§5.2 求解结论 → 真实实现）

`test_double_buffer_pipeline.py` **v2**：四模式真实现（V0 基线 / V1 精简链 / V4 批量提交 /
V5 同流顺序）+ **按负载自动选型**。

| n | 基准 | V0 | V1 | V4 | V5 | 选型 |
|---|---|---|---|---|---|---|
| 512 | 0.960 | -41.7% | -16.6% | +6.5% | **+33.1%** | V5（小负载不流水线） |
| 1024 | 1.340 | -0.1% | +15.7% | **+27.0%** | +18.1% | V4（压同步次数） |
| 2048 | 3.873 | **+38.8%** | +38.1% | +12.8% | +6.9% | V0（保留批间流水） |

判定 `DBUF2_PASS`。**训练侧同步回补**：`pin_memory=True` + `batch.to(dev, non_blocking=True)`，
100 步完整复测无崩溃（loss 2.64→1.37、tok/s 5166、HBM 无泄漏、进程残留 0）→ 训练职责 **11/11 全绿**。

### 3. 多流 Stream 职责子项全量核查（16 项）

新增 `probe_stream_semantics_full.py`，`STREAM_SEMANTICS_PASS 8/8`。
此前 **S-8 ~ S-13 共 6 项从未覆盖**，且既有 S1/S2 用例为"框架层近似"（未真正创建流）：

| 子项 | 结论 |
|---|---|
| S-8 默认流 vs 命名流 | 默认流/命名流各自正确；**`record_stream` 后跨流复用正确** |
| S-9 流错误隔离（分层） | **API 调用级已实测**（流A 注入 107015 后流B 正常）；⚠️ **设备级为语义推断**（AICORE_TIMEOUT/EXCEPTION 影响该设备全部流，需设备级恢复） |
| S-10 流/事件生命周期 | 500 次创建销毁无配额泄漏 |
| S-11 跨流内存分配 | 依赖后跨流使用正确 |
| S-12 流优先级 | `leastPriority=7 / greatestPriority=0`（**值越小优先级越高**，范围 0~7） |
| S-13 多设备流绑定 | 双设备流不互串 |

**两条工程纪律（本次查明）**：
1. **`record_stream` 硬纪律**：PyTorch 缓存分配器跨流复用内存必须
   `tensor.record_stream(using_stream)`，否则内存可能被提前回收重用 → 数据竞争。
   （已据此修复双缓冲 v2 实现缺口）
2. **错误隔离分层**：API 级错误只影响该次调用；芯片级错误影响该设备全部流，
   恢复须走设备级 `aclrtResetDevice`，流级重试无效。

### 4. 其他新增

| 项 | 成果 |
|---|---|
| graph capture 流语义 | `GRAPH_CAPTURE_PASS 5/5`；**踩坑：capture 只记录不执行，必须先 replay 才产生结果** |
| D11 真实重建落地 | `recovery.py` 新增 `rebuild_mode=real/hybrid`（CANN 官方 `aclrtResetDevice` 序列）；多进程联调 `MULTIPROC_REBUILD_PASS`（同卡服务进程 30/30 轮零失败，**隔离性成立**） |
| SIGKILL 释放 | `kill -9` 后进程残留 0、HBM 完全释放 → **坑 A2 的"SIGKILL 残留占位"在当前环境未复现** |
| 长驻显存 | serve 21 分钟窗口 HBM/RSS **零增长**，无泄漏 |
| timeout 真实触发 | `TIMEOUT_REALTIME_PASS`：stream 同步超时真实返回 **507046** → L3_EXECUTION 与映射表一致（三层验证 L3 层补齐） |
| 回归 | conformance **13/13** + infer **6/6** PASS（多轮） |

### 5. 遗留（不计入本次 PR 阻塞项）

- **D11 real 模式**：本地多进程联调已过，生产默认启用前**需大模型多卡多进程压力测试调优**
- **S-12 调度效果**、**S-9 设备级错误真实触发**：需压测环境
- 全景文档：`docs/ASCEND_910C_DC_STREAM_MAPPING_20260902.md`（含每项原理/实现/判据）
