# ascend-infer-vllm v1 —— 离线归档

> 官方公共镜像，`docker pull <digest>` 随时可拉，一般无需离线包。

## 若本机保留离线副本

| 文件 | sha256 | 说明 |
|---|---|---|
| （可选）`vllm-ascend-v0.20.2rc1-a3.tar.gz` | 保留时登记 | 按 digest `sha256:5cf8a2b6…` 保存，防 quay 上 rc 标签被 GC；连带留存拷出的 `triton_ascend 3.2.1` wheel |

本机实体路径（若有）见同目录 `BACKUP.local`（不追踪）。
