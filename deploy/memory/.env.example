# memory 子方向环境变量配置（cp 成 .env 后编辑；.env 不入仓，每人一份）
# 公共配置（设备/挂载/公共环境变量）见 deploy/compose.base.yml，无需在此重复

# 本人宿主路径（每人改成自己的；容器内布局固定 /workspace/<repo>，宿主路径随意）
WORKSPACE_HOST=/home/xliu969/workspace

# memory 子方向专属：显存池开关（默认开启）
FLAGOS_USE_CACHING_ALLOCATOR=true
